<% content_for :title, "Resend Unlock Instructions" %>

<%= render UI::CardComponent.new(title: "Resend unlock instructions", class: "max-w-md mx-auto") do |card| %>
  <% card.with_body do %>
    <p class="text-center text-base-content/70 mb-6">Enter your email address and we'll send you instructions to unlock your account.</p> <%# Moved subtitle here %>

    <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-4" }) do |f| %>
      <%= f.error_notification %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <%= f.full_error :unlock_token %>

      <%= f.input :email,
                  required: true,
                  autofocus: true,
                  label: "Email address",
                  input_html: {
                    class: "input input-bordered w-full",
                    autocomplete: "email"
                  } %>

      <%= f.button :submit, "Resend Unlock Instructions", class: "btn btn-primary w-full" %>
    <% end %>

    <div class="text-center mt-6">
      <%= render "devise/shared/links" %>
    </div>
  <% end %> <%# card.with_body %>
<% end %>
